#!/usr/bin/env python

import utils

# returns 
def get_sum_length(i, s):
	for j in xrange(1, i):


def main():
	primes = utils.get_primes(10**6)
	maxLength = 0
	print len([x for x in primes if x])
	for i in xrange(0,10**6+1):
		for size in xrange(0,i):
			
	return maxLength

main()